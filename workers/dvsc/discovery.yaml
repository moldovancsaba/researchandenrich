tenant: dvsc
operation: discovery
prompt: prompts/discovery/dvsc.md
schedule:
  kind: every
  everyMs: 3600000
retry:
  maxAttempts: 3
  backoffMs: 5000
timeoutMs: 300000
dependencies: []
healthCheck:
  endpoint: GET /api/leads?brand=dvsc&limit=1
  expectedStatus: 200
