# DVSC ContentCreator credentials
# Placeholder values only -- this file was scaffolded to match .env.cogmap/.env.seyu's
# exact shape, not populated with real secrets (none were available to generate this).
# Whoever has SLG_API_KEY/DVSC_MONGODB_URI access must fill these in before the
# dvsc-discovery/dvsc-enrichment cron entries can actually run.
# Set file permissions to 600 after filling in, matching .env.cogmap/.env.seyu.
export SLG_API_KEY=REPLACE_WITH_REAL_SLG_API_KEY
export DVSC_MONGODB_URI=REPLACE_WITH_REAL_DVSC_MONGODB_URI
