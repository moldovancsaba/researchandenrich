# Master Cron Schedule - Auto-generated from worker definitions
# Do not edit manually — run `node config/cron-generator.js` to regenerate
# Source of truth: workers/*/ discovery.yaml and workers/*/enrichment.yaml
# Generated by: config/cron-generator.js

schedule:
  - worker: cogmap-discovery
    tenant: cogmap
    app: researchandenrich
    operation: discovery
    prompt: prompts/discovery/cogmap.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: cogmap-enrichment
    tenant: cogmap
    app: researchandenrich
    operation: enrichment
    prompt: prompts/enrichment/cogmap.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: seyu-discovery
    tenant: seyu
    app: researchandenrich
    operation: discovery
    prompt: prompts/discovery/seyu.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: seyu-enrichment
    tenant: seyu
    app: researchandenrich
    operation: enrichment
    prompt: prompts/enrichment/seyu.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: dvsc-discovery
    tenant: dvsc
    app: researchandenrich
    operation: discovery
    prompt: prompts/discovery/dvsc.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: false
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: dvsc-enrichment
    tenant: dvsc
    app: researchandenrich
    operation: enrichment
    prompt: prompts/enrichment/dvsc.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: false
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: classscout-discovery
    tenant: classscout
    app: classscout
    operation: discovery
    prompt: prompts/discovery/classscout.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

  - worker: classscout-enrichment
    tenant: classscout
    app: classscout
    operation: enrichment
    prompt: prompts/enrichment/classscout.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
    timeoutMs: 300000
    retry:
      maxAttempts: 3
      backoffMs: 5000

