# Master Cron Schedule - Auto-generated from worker definitions
# Do not edit manually — run `make cron-generate` to regenerate
# Source of truth: workers/*/discovery.yaml and workers/*/enrichment.yaml
# Generated by: config/cron-generator.js

schedule:
  # CogMap - Sales lead tenant
  - worker: cogmap-discovery
    tenant: cogmap
    app: salesleadgenerator
    operation: discovery
    prompt: prompts/discovery/cogmap.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true

  - worker: cogmap-enrichment
    tenant: cogmap
    app: salesleadgenerator
    operation: enrichment
    prompt: prompts/enrichment/cogmap.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true

  # Seyu - Sales lead tenant
  - worker: seyu-discovery
    tenant: seyu
    app: salesleadgenerator
    operation: discovery
    prompt: prompts/discovery/seyu.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true

  - worker: seyu-enrichment
    tenant: seyu
    app: salesleadgenerator
    operation: enrichment
    prompt: prompts/enrichment/seyu.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true

  # ClassScout API - Program research tenant
  - worker: classscout-api-discovery
    tenant: classscout-api
    app: classscout-api
    operation: discovery
    prompt: prompts/discovery/classscout-api.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true

  - worker: classscout-api-enrichment
    tenant: classscout-api
    app: classscout-api
    operation: enrichment
    prompt: prompts/enrichment/classscout-api.md
    cron: "*/45 * * * *"
    tz: "Europe/Budapest"
    enabled: true
